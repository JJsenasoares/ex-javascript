var altura = Number( prompt("Me de sua altura"))
var base = Number( prompt("Me de sua base"))

var resultado = area1 * area2 / 2 

alert(`Base = ${base}
    Altura = $(altura)
    
    A area do triamgulo = ${area}`)
